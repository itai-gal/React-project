import { DynamicPageHeader } from "../components/DynamicPageHeader"
import { MainLayout } from "../layouts/MainLayout"

export const Favorites = () => {
    return <MainLayout><DynamicPageHeader header="Favorites" /></MainLayout>
}